# Plugin Architecture

This directory contains documentation for the Librarian plugin system. Plugins extend the system's ability to analyze, extract, and enrich content from various file types.

---

## Plugin Philosophy

Plugins in Librarian follow these core principles:

1. **Single Responsibility**: Each plugin handles one type of content analysis
2. **Stateless Processing**: Plugins process content and generate metadata without maintaining state between runs
3. **Deterministic Output**: Same input always produces same output (for metadata extraction)
4. **Explicit Configuration**: Plugins are enabled/disabled via configuration, not hardcoded

---

## Plugin Lifecycle

```
File
  ↓
Plugin
  ↓
Metadata
  ↓
Database
  ↓
Search Index
  ↓
User Query
```

### Lifecycle Stages

| Stage | Description |
|-------|-------------|
| **File** | Document enters the system via discovery |
| **Plugin** | Plugin processes the file based on its artifact type |
| **Metadata** | Plugin generates structured metadata |
| **Database** | Metadata is persisted to the appropriate table |
| **Search Index** | Metadata is indexed for retrieval |
| **User Query** | User searches and retrieves results |

---

## Plugin Responsibilities

Each plugin is responsible for:

1. **Input Validation**: Verify the file type and structure are supported
2. **Content Processing**: Extract or analyze the file content
3. **Metadata Generation**: Create structured, searchable metadata
4. **Error Handling**: Gracefully handle corrupt or unsupported files

---

## Plugin Inputs

Plugins receive files based on their configured artifact types:

| Plugin | Supported Inputs |
|--------|------------------|
| EXIF | jpg, jpeg, heic, png, webp, tiff |
| Object Detection | jpg, jpeg, heic, png, webp |
| OCR | jpg, jpeg, png, pdf |
| PDF | pdf |
| Video | mp4, mov, avi, mkv |
| Audio | mp3, wav, flac, m4a |
| Transcription | mp3, wav, m4a, mp4 |
| Face Detection | jpg, jpeg, png, webp |
| Similar Image Search | jpg, jpeg, png, webp |
| Duplicate Detection | All file types |

---

## Plugin Outputs

Plugins generate these types of outputs:

### Artifacts

Persistent files generated by plugins:

| Plugin | Artifacts |
|--------|-----------|
| EXIF | metadata, thumbnails, GPS coordinates |
| Object Detection | bounding boxes, labels |
| OCR | text layer |
| PDF | extracted text |
| Thumbnail Generation | preview images |

### Searchable Metadata

Structured data stored in the database:

| Plugin | Searchable Metadata |
|--------|---------------------|
| EXIF | camera, lens, timestamp, GPS, device |
| Object Detection | object labels, confidence scores |
| OCR | extracted text |
| Transcription | spoken text |

---

## Standard Plugin Structure

Every plugin should follow this documentation structure:

```
docs/plugins/<plugin-name>/
├── README.md              # Overview, inputs, outputs, status
├── plugin-contract.md      # Integration requirements (for analysis plugins)
├── engines.md             # Supported engines (for detection plugins)
├── capabilities.md        # Detailed capabilities and limitations
├── metadata-schema.md     # JSON schema for plugin output
├── architecture.md        # Technical architecture details
├── roadmap.md            # Future improvements and features
└── datasets.md           # Training/test datasets (if applicable)
```

---

## Plugin Examples

### EXIF Plugin

```
Image
  ↓
EXIF Parser
  ↓
Metadata
  ↓
Database Tags
```

### Object Detection Plugin

```
Image
  ↓
YOLO Model
  ↓
Objects
  ↓
Database Tags
```

### PDF Plugin

```
PDF
  ↓
Text Extraction
  ↓
Content
  ↓
Search Index
```

### OCR Plugin

```
Image
  ↓
OCR Engine
  ↓
Text
  ↓
Search Index
```

---

## Available Plugins

| Plugin | Status | Description |
|--------|--------|-------------|
| [EXIF](./exif/) | ✅ Implemented | Extract photo metadata from images |
| [Object Detection](./object-detection/) | 📋 Planned | Detect objects in images |
| OCR | 📋 Planned | Extract text from images |
| PDF | 📋 Planned | Extract content from PDFs |
| Video | 📋 Planned | Process video files |
| Audio | 📋 Planned | Process audio files |
| Transcription | 📋 Planned | Convert speech to text |
| Face Detection | 📋 Planned | Detect faces in images |
| Similar Image Search | 📋 Planned | Find visually similar images |
| Duplicate Detection | 📋 Planned | Find duplicate files |

---

## Plugin Configuration

Plugins are configured in `config/plugins.yaml`:

```yaml
plugins:
  photo_metadata:
    enabled: true
    job_type: extract_photo_metadata
  thumbnail:
    enabled: false
    job_type: generate_thumbnail
  ocr:
    enabled: false
    job_type: run_ocr
  object_detection:
    enabled: false
    job_type: object_detection
```

---

## Plugin Registry

The `registry/plugin_registry.py` module provides plugin management:

- Enable/disable plugins at runtime via environment variables
- Validate plugin/handler alignment at startup
- Prevent scheduling of jobs without registered handlers

See [P13-P15 Plugin Architecture](../refactor/P13-P15-plugin-architecture.md) for implementation details.

---

## Creating a New Plugin

To add a new plugin:

1. Create `docs/plugins/<plugin-name>/` directory structure
2. Add `README.md`, `capabilities.md`, `metadata-schema.md`, `architecture.md`, `roadmap.md`
3. Register the plugin in `config/plugins.yaml`
4. Implement the worker handler in `workers/`
5. Update `registry/plugin_registry.py` if needed

---

## Related Documentation

- [Plugin Registry Implementation](../refactor/P13-P15-plugin-architecture.md)
- [Operation EXIF](../refactor/operation-exif/)
- [API Contract](../api-contract/)
- [Architecture](../architecture/)